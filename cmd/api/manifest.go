package main

var (
	// Application Slug
	AppSlug = "running-app-api"
	// Application Name
	AppName = "Running App API"
	// AppVersion value will be override during automated build
	AppVersion = "local.version"
	// CommitHash value will be override during automated build
	CommitHash = "local.build"
)
